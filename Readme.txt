This setup is to use the soketi web socket backend server with angular front-end for web socket connection and listning on specific channels.

Setup is entirly deployed using the docker, so you need to have docker installed in your system.
